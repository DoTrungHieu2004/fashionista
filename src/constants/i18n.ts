/**
 * AsyncStorage key for the user's persisted language preference.
 * Namespaced to avoid collisions with other libraries using AsyncStorage.
 */
export const LANGUAGE_STORAGE_KEY = '@fashionista/language';
