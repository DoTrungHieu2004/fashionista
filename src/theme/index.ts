import { themeColors } from './themeColor';
import { semanticColors, neutralColors } from './color';
import { spacing } from './spacing';
import { rounded } from './rounded';
import { typography } from './typography';

export { themeColors, semanticColors, neutralColors, spacing, rounded };

export const baseThemeTokens = { spacing, rounded, typography };
